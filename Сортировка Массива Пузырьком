Сортировка Массива
Необходимо реализовать алгоритм, который сортируем массив целых чисел по порядку

Пример тест кейса: [7, 1, 5, 3, 6, 4]

// На Клиенте

	Массив2 = Новый Массив();
	Массив2.Добавить(7);
	Массив2.Добавить(1);
	Массив2.Добавить(5);
	Массив2.Добавить(3);
	Массив2.Добавить(6);
	Массив2.Добавить(4);
	
	Серверный.СортировкаПоПорядку(Массив2);

// На сервере
Функция СортировкаПоПорядку(ВходныеДанные) Экспорт
	Для i = 0 По ВходныеДанные.ВГраница() Цикл
		Для j = 0 ПО ВходныеДанные.Вграница() - i - 1 Цикл
			Если ВходныеДанные[j] > ВходныеДанные[j + 1] Тогда
				Замена = ВходныеДанные[j];
				ВходныеДанные[j] = ВходныеДанные[j + 1];
				ВходныеДанные[j + 1] = Замена;
			КонецЕсли;            
		КонецЦикла;        
	КонецЦикла;    
	
	Для Каждого Элемент Из ВходныеДанные Цикл
		Сообщить(Элемент);	
	КонецЦикла;
	
КонецФункции

