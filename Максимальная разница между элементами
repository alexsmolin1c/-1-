Максимальная разница между элементами
Вам необходимо написать программу на языке программирования 1С, 
чтобы найти максимальную разницу между двумя элементами в массиве. 
Разница должна быть между элементами, где элемент с меньшим индексом 
идет до элемента с большим индексом. 
Формально, дан массив целых чисел, и вы должны найти максимальное 
значение arr[j] - arr[i], где 0 ≤ i < j < n, а n - размер массива. 
Входные данные содержатся в переменной “ВходныеДанные”. 
Результат необходимо поместить в переменную “Результат”

Пример тест кейса: [7, 1, 5, 3, 6, 4]

// На Клиенте

	Массив2 = Новый Массив(6);
	Массив2[0] = 7;
	Массив2[1] = 1;
	Массив2[2] = 5;
	Массив2[3] = 3;
	Массив2[4] = 6;
	Массив2[5] = 4;

	Серверный.НайтиМаксимальнуюРазницуМеждуЭлементами(Массив2);

// На Сервере

Функция НайтиМаксимальнуюРазницуМеждуЭлементами(ВходныеДанные) Экспорт
    // Получаем размер массива
    РазмерМассива = ВходныеДанные.Количество();

    // Переменная для хранения максимальной разницы
    МаксимальнаяРазница = 0;
	
    // Переменные для хранения минимального значения и его индекса
    МинимальноеЗначение = ВходныеДанные[0];
    ИндексМинимальногоЭлемента = 0;

    // Проходим по массиву начиная со второго элемента
    Для Индекс = 1 По РазмерМассива - 1 Цикл
        // Если текущий элемент меньше минимального значения,
        // обновляем минимальное значение и его индекс
        Если ВходныеДанные[Индекс] < МинимальноеЗначение Тогда
            МинимальноеЗначение = ВходныеДанные[Индекс];
            ИндексМинимальногоЭлемента = Индекс;
        КонецЕсли;

        // Вычисляем разницу между текущим элементом и минимальным значением
        ТекущаяРазница = ВходныеДанные[Индекс] - МинимальноеЗначение;
        // Если текущая разница больше максимальной, обновляем максимальную разницу
        Если ТекущаяРазница > МаксимальнаяРазница Тогда
            МаксимальнаяРазница = ТекущаяРазница;
        КонецЕсли;
    КонецЦикла;

    // Помещаем результат в переменную
    Результат = МаксимальнаяРазница;
	Сообщить("Максимальная разница между элементами: " + Результат);

	Возврат Результат;
КонецФункции

