Подстрока максимальной длинны
Дана строка на входе, необходимо найти длину максимальной подстроки

Пример 1: Вход: s = "abcabcbb" Результат: 3 Объяснение: Ответ "abc", максимальная длинна 3.
Пример 2: Вход: s = "bbbbb" Результат: 1 Объяснение: Ответ "b", максимальная длинна 1.
Пример 3: Вход: s = "pwwkew" Результат: 3 Объяснение: Ответ "wke", максимальная длинна 3.

// На Клиенте
	//Серверный.НайтиДлинуМаксимальнойПодстроки("Поралоыивмдваобимбывоаим");
	//Серверный.НайтиДлинуМаксимальнойПодстроки("abcabcbb");
	//Серверный.НайтиДлинуМаксимальнойПодстроки("bbbbb");
	//Серверный.НайтиДлинуМаксимальнойПодстроки("pwwkew");

// На Сервере
Функция НайтиДлинуМаксимальнойПодстроки(Строка) Экспорт
	Результат = 0;
	Подстрока = "";
  ДлинаСтроки = СтрДлина(Строка);
	Для Индекс = 1 По ДлинаСтроки Цикл
	    Символ = Сред(Строка, Индекс, 1);
	    Если Найти(Подстрока, Символ) = 0 Тогда
	        Подстрока = Подстрока + Символ;
	        Результат = 1; // Если есть хоть один символ, то результат будет 1
	    КонецЕсли;
	КонецЦикла;
  Сообщить(Результат);
	Возврат Результат;
	
	Результат = 0;
    Подстрока = "";
    ДлинаСтроки = СтрДлина(Строка);

    Для Индекс = 1 По ДлинаСтроки Цикл
        Символ = Сред(Строка, Индекс, 1);
        
        Если Найти(Подстрока, Символ) = 0 Тогда
            Подстрока = Подстрока + Символ;
			Результат = Макс(Результат, СтрДлина(Подстрока));

                    Иначе
            Позиция = Найти(Подстрока, Символ);
            Подстрока = Сред(Подстрока, Позиция + 1) + Символ;
        КонецЕсли;
    КонецЦикла;

	Результат = Макс(Результат, СтрДлина(Подстрока));
    Сообщить(Результат);
	Сообщить(Подстрока);

    Возврат Результат;
	
	КонецФункции

