Дубли нулей
На вход функции подается целочисленный массив фиксированной длины arr, 
дублируйте каждое вхождение нуля, перемещая остальные элементы вправо. 
Обратите внимание, что элементы, превышающие длину исходного массива, не записываются.
Вносите вышеуказанные изменения в входной массив и ничего не возвращайте.

Пример 1: Вход: arr = [1,0,2,3,0,4,5,0] Выход: [1,0,0,2,3,0,0,4]
Объяснение: После вызова функции входной массив изменяется на: [1,0,0,2,3,0,0,4] 
Пример 2: Ввод: arr = [1,2,3] Выход: [1,2,3]
Объяснение: После вызова вашей функции входной массив изменяется на: [1,2,3]

Решение: 
// На Клиенте
	МассивНаВход = Новый Массив();
	МассивНаВход.Добавить(1);
	МассивНаВход.Добавить(0);
	МассивНаВход.Добавить(2);
	МассивНаВход.Добавить(3);
	МассивНаВход.Добавить(0);
	МассивНаВход.Добавить(4);
	МассивНаВход.Добавить(5);
	МассивНаВход.Добавить(0);

	Серверный.Основное(МассивНаВход);

// На Сервере

Функция ДублируетНулиВМассиве(ВходныеДанные) Экспорт
	Для Индекс = 0 По ВходныеДанные.Количество() - 1 Цикл
		Если ВходныеДанные[Индекс] = 0 Тогда
			ВходныеДанные.Вставить(Индекс + 1, 0);
			Индекс = Индекс + 1;
			ВходныеДанные.Удалить(ВходныеДанные.Количество() - 1);
		КонецЕсли;
	КонецЦикла;
	Возврат ВходныеДанные;
	// Для проверки
	Для Каждого Элемент Из ВходныеДанные Цикл
		Сообщить(Элемент);	
	КонецЦикла;
КонецФункции
